# Core scientific stack
numpy>=2.2.6
pandas>=2.3.3
scipy>=1.15.3
xarray>=2025.6.1
netCDF4>=1.7.4
h5py>=3.16.0
zarr>=2.18.3
dask[complete]>=2026.8.0
pyarrow>=25.0.1           # parquet I/O for the unified database

# Geospatial
rasterio>=1.4.4
rioxarray>=0.19.0
geopandas>=1.1.4
shapely>=2.1.2
pyproj>=3.7.1
h3>=4.5.0                 # hex grid indexing (optional aggregation)
regionmask>=0.13.0

# Earth Engine (server-side data access)
earthengine-api>=1.7.42
geemap>=0.37.2
eemont>=2025.7.1             # convenience for S5P/ERA5 collection handling

# Ground / reanalysis access (non-GEE fallbacks)
cdsapi>=0.7.7             # ERA5 boundary-layer height via Copernicus CDS
requests>=2.34.2
beautifulsoup4>=4.15.0    # CPCB / MOSDAC scraping helpers

# Machine learning
scikit-learn>=1.7.2
xgboost>=3.2.0
torch>=2.14.0              # CNN / LSTM / CNN-LSTM
torchvision>=0.29.0

# Spatial statistics (HCHO hotspots)
esda>=2.7.0               # Getis-Ord Gi* (PySAL)
libpysal>=4.13.0          # spatial weights for Gi*
pointpats>=2.5.2

# Transport / trajectories
# pysplit / HYSPLIT are installed separately (see docs/13_transport_analysis.md)
windrose>=1.10.0

# Visualization
matplotlib>=3.10.9
cartopy>=0.25.0
contextily>=1.7.1
seaborn>=0.13.2
plotly>=7.0.0

# Dashboard
streamlit>=1.63.0
folium>=0.20.0
streamlit-folium>=0.26.2

# Config / utilities / quality
pyyaml>=6.0.3
pydantic>=2.13.5
tqdm>=4.70.0
loguru>=0.7.3
typer>=0.27.2              # CLI for pipeline scripts

# Dev / test
pytest>=9.1.1
ruff>=0.16.6
