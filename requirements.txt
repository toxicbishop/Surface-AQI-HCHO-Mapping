# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
xarray>=2024.1
netCDF4>=1.6
h5py>=3.10
zarr>=2.16
dask[complete]>=2024.1
pyarrow>=15.0           # parquet I/O for the unified database

# Geospatial
rasterio>=1.3
rioxarray>=0.15
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
h3>=4.0                 # hex grid indexing (optional aggregation)
regionmask>=0.11

# Earth Engine (server-side data access)
earthengine-api>=0.1.390
geemap>=0.32
eemont>=0.3             # convenience for S5P/ERA5 collection handling

# Ground / reanalysis access (non-GEE fallbacks)
cdsapi>=0.7             # ERA5 boundary-layer height via Copernicus CDS
requests>=2.31
beautifulsoup4>=4.12    # CPCB / MOSDAC scraping helpers

# Machine learning
scikit-learn>=1.4
xgboost>=2.0
torch>=2.2              # CNN / LSTM / CNN-LSTM
torchvision>=0.17

# Spatial statistics (HCHO hotspots)
esda>=2.5               # Getis-Ord Gi* (PySAL)
libpysal>=4.10          # spatial weights for Gi*
pointpats>=2.4

# Transport / trajectories
# pysplit / HYSPLIT are installed separately (see docs/13_transport_analysis.md)
windrose>=1.9

# Visualization
matplotlib>=3.8
cartopy>=0.22
contextily>=1.5
seaborn>=0.13
plotly>=5.18

# Dashboard
streamlit>=1.31
folium>=0.15
streamlit-folium>=0.18

# Config / utilities / quality
pyyaml>=6.0
pydantic>=2.6
tqdm>=4.66
loguru>=0.7
typer>=0.9              # CLI for pipeline scripts

# Dev / test
pytest>=8.0
ruff>=0.3
